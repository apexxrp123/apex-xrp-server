name: decode-xaman-keepclear
on:
  push:
    branches: [main]
    paths:
      - "apex-io/_xaman_b64/**"
      - ".github/workflows/decode-xaman-keepclear.yml"
  workflow_dispatch:
jobs:
  decode:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Assemble decode and commit xaman.js
        run: |
          set -euo pipefail
          cat apex-io/_xaman_b64/p*.txt > /tmp/xaman.b64
          python3 - <<'PY2'
          import zlib, base64, hashlib, pathlib
          b = pathlib.Path("/tmp/xaman.b64").read_text().strip()
          data = zlib.decompress(base64.b64decode(b))
          expected = pathlib.Path("apex-io/_xaman_b64/sha256.txt").read_text().strip()
          got = hashlib.sha256(data).hexdigest()
          if got != expected:
              raise SystemExit(f"sha256 mismatch: {got} != {expected}")
          pathlib.Path("xaman.js").write_bytes(data)
          print("wrote", len(data), "bytes", got)
          PY2
          rm -rf apex-io/_xaman_b64
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && echo "no change" && exit 0
          git commit -m "Keep clearing Max payloads: skip uncancellable UUIDs and try the next cited one"
          git push
